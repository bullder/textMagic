{% extends 'base.html.twig' %}

{% block title %}Test{% endblock %}

{% block body %}
   <h1>Select the test:</h1>
    <ul>
         {% for test in tests %}
              <li>
                <a href="{{ path('test', {'id': test.id}) }}">{{ test.title }}</a>
              </li>
         {% endfor %}
    <ul>
{% endblock %}